language: python
python: 
    - "3.7"
before_install:
  - cd arcospy/tests/
  - "pip install -U pip"
  - "pip install -U arcospy"
  - "export PYTHONPATH=$PYTHONPATH:$(pwd)"
install:
  - pip install -r requirements.txt
script: python test_majority.py